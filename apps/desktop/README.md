# Desktop App Boundary

`apps/desktop` is the reserved desktop shell boundary. Tauri/window, notification, file-picker,
update, secret-store bridge, and sidecar lifecycle code belong here. Agent and Provider business
logic remains in backend services.
