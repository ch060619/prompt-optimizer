# Agent Core Boundary

`backend/rabbit_code` is the reserved Rabbit Code Agent Core boundary. The existing
`prompt_optimizer` package remains the active compatibility implementation during the staged
migration.
